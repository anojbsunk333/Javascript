// For loop 

// The for statement creates a loop with 3 optional expressions: 

// Example: 

const cars = ["BMW", "Volvo", "Saab", "Ford"];
let len = cars.length;

let text = "";
for (let i = 0; i < len; i++) {
  text += cars[i];
}